import React from "react";
import { Title } from "../../components/template";
import { Page } from "../../interfaces";

export function Customers(props: Page) {
  return <Title title={props.title} />
}
